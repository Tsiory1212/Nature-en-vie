{% extends 'base_admin.html.twig' %}

{% block title %}Ajout de classe{% endblock %}

{% block body %}

<div class="container mt-5">
    <h1>{{label|default('Ajout') }} classe </h1>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn {{btn_class|default('btn-primary')}}">{{button_label|default('Ajouter')}}</button>
        {{ form_end(form) }}
</div>
{% endblock %}
